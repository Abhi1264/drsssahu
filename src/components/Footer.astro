---
import { site } from "../config/site";
import { verifiedProfiles, visibleItems } from "../utils/content";
import { externalLinkAttrs } from "../utils/links";
import Container from "./Container.astro";

const footerNav = visibleItems(site.footer.links);
const profiles = verifiedProfiles();
---

<footer class="mt-20 border-t border-rule bg-surface/60 pb-[env(safe-area-inset-bottom)]" transition:name="site-footer">
  <Container class="grid gap-10 py-14 md:grid-cols-3">
    <div>
      <p class="font-serif text-xl text-ink">{site.person.name}</p>
      <p class="mt-3 max-w-sm text-sm leading-relaxed text-muted">{site.footer.tagline}</p>
    </div>
    <nav aria-label="Footer">
      <p class="text-xs tracking-[0.16em] text-muted uppercase">On this site</p>
      <ul class="mt-3 space-y-1 text-sm">
        {footerNav.map((item) => (
          <li>
            <a href={item.href} {...externalLinkAttrs(item.href)} class="inline-flex min-h-11 items-center text-ink-soft no-underline hover:text-navy hover:underline md:min-h-0 md:py-1">
              {item.label}
            </a>
          </li>
        ))}
      </ul>
    </nav>
    <div>
      <p class="text-xs tracking-[0.16em] text-muted uppercase">Research profiles</p>
      <ul class="mt-3 space-y-1 text-sm">
        {profiles.map((item) => (
          <li>
            <a
              href={item.url}
              {...externalLinkAttrs(item.url)}
              class="inline-flex min-h-11 items-center text-ink-soft no-underline hover:text-navy hover:underline md:min-h-0 md:py-1"
            >
              {item.label}
            </a>
          </li>
        ))}
      </ul>
    </div>
  </Container>
  <div class="border-t border-rule">
    <Container class="py-5">
      <p class="text-sm text-muted">
        © {new Date().getFullYear()} {site.footer.copyrightName}
      </p>
    </Container>
  </div>
</footer>
